{
  "name": "typescript-test",
  "version": "0.0.1",
  "description": "A quick test to fiddle with typescript in node.js",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node .",
    "test": "echo Nope... && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "github.com/brendan0powers/node-typescript-test"
  },
  "author": "Brendan Powers",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/node": "^12.0.8",
    "@types/express": "^4.17.0",
    "tslint": "^5.17.0",
    "typescript": "^3.5.2"
  }
}
